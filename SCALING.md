# 横向扩展设计草案（从单机单进程到多 worker / 多机）

> 状态:**设计草案,尚未实施**。当前生产形态是「单 VM + 单 uvicorn worker」(见 [DEPLOYMENT.md](DEPLOYMENT.md))。
> 本文回答一个问题:当单进程扛不住时,**按什么顺序、改哪些地方**才能安全地加 worker / 加机器。

## 1. 为什么现在不能直接加 worker

四处进程内 / 本地文件状态,是横向扩展的硬约束。多 worker 或多机会各自持有一份,导致行为不一致或数据损坏:

| 组件 | 代码位置 | 现状 | 加 worker 后的故障 |
|------|----------|------|--------------------|
| 会话历史 | `session_manager.py` | 进程内存 dict | 同一用户的连续提问被路由到不同 worker → 上下文丢失 |
| 登录限流 | `rate_limit.py` | 进程内存 | 每个 worker 各算各的 → 实际限流阈值被放大 N 倍,防爆破失效 |
| 用户库 | `user_store.py` (SQLite `users.db`) | 本地单文件 | 多机无法共享;SQLite 高并发写锁竞争 |
| 向量库 | `vector_store.py` (ChromaDB 本地持久化) | 本地目录 | 多进程并发写不安全;多机各持一份不一致 |

> 注:LLM 调用(DeepSeek)本身是无状态的外部 HTTP,不构成扩展瓶颈——瓶颈全在上面这四处本地状态。

## 2. 目标架构

```
            ┌──────────── 负载均衡 / nginx (sticky 可选) ───────────┐
            │                                                        │
      ┌─────▼─────┐   ┌───────────┐   ┌───────────┐                 │
      │ app 实例1 │   │ app 实例2 │   │ app 实例N │  (无状态,可水平扩)
      └─────┬─────┘   └─────┬─────┘   └─────┬─────┘
            │               │               │
   ┌────────┼───────────────┼───────────────┼─────────┐
   │        ▼               ▼               ▼          │
   │   ┌─────────┐    ┌──────────────┐  ┌──────────────────┐
   │   │  Redis  │    │  Postgres    │  │ Chroma Server /   │
   │   │ 会话+限流│    │  (用户/账号) │  │ Qdrant / pgvector │
   │   └─────────┘    └──────────────┘  └──────────────────┘
   └──────────────── 共享状态层(有状态,各自独立扩展) ──────────┘
```

核心原则:**把 app 进程改造成无状态**,所有状态外移到独立的、可各自扩展的后端。

## 3. 分阶段路线(按收益/成本排序,可逐步上)

### 阶段 0 — 现状(单机单进程)
适用:内部工具、并发低。**先把垂直扩展吃满**(更大机器、`--workers 1` 下 FastAPI 仍并发处理 I/O 密集请求)再考虑后面。

### 阶段 1 — 会话 + 限流外移到 Redis（解锁多 worker)
> 这是性价比最高的一步,改动小,直接解锁单机多 worker。

- **会话**:`SessionManager` 抽象成接口,新增 `RedisSessionStore`(键 `session:{id}`,`HASH` 存历史,`EXPIRE` 做 TTL)。`MAX_HISTORY` 截断逻辑不变。
- **限流**:`RateLimiter` 换成 Redis 实现(`INCR` + `EXPIRE` 滑动窗口,或 `redis-cell`)。这样限流跨 worker / 跨机生效。
- 配置:新增 `REDIS_URL`;无该变量时回退到现有的进程内实现(保持单机零依赖可跑)。
- 完成后即可 `--workers N`(单机)。

### 阶段 2 — 用户库迁到 Postgres（解锁多机)
- `user_store.py` 已经是窄接口(增删查用户、校验密码、bump `token_version`),换成 Postgres 仅替换实现。bcrypt 哈希、`token_version` 语义不变。
- JWT 已是无状态的(`ver` 版本号校验),app 实例之间天然共享,无需 session 亲和。
- 配置:`DATABASE_URL`;保留 SQLite 作为本地开发默认。

### 阶段 3 — 向量库改为服务化（解锁多机一致 + 并发写)
- 从「嵌入式 ChromaDB(本地目录)」改为**独立向量服务**,候选:
  - **Chroma Server**(改动最小,API 基本一致,`chromadb.HttpClient`)
  - **Qdrant / Weaviate**(更成熟的生产特性)
  - **pgvector**(若已上 Postgres,可少一个组件)
- `vector_store.py` 的两集合设计(`course_catalog` + `course_content`)和两步检索逻辑保持不变,仅换 client。
- 摄取(`document_processor`)改为指向远端集合;多实例共享同一向量库,文档只需摄取一次。
- 配置:`CHROMA_HOST`/`CHROMA_PORT` 或 `VECTOR_BACKEND`。

### 阶段 4 — 运维与弹性
- app 实例容器化(Dockerfile),用 compose / k8s 编排,按 CPU/QPS 自动扩缩。
- 文档摄取从「app 启动时同步加载」改为**独立的一次性 job**(避免每个实例启动都扫一遍 `docs/`,且摄取与服务解耦)。
- 健康检查端点(`/healthz`)区分「存活」与「依赖就绪(Redis/PG/向量库可连)」。
- DeepSeek 调用加超时、重试、熔断;监控其延迟与错误率(它是唯一的外部强依赖)。

## 4. 改造时的不变量(避免回归)

- **JWT `token_version` 语义**:改密必须 +1 并立即让旧 token 失效——迁库时务必保留该列与校验。
- **ChromaDB 元数据不能为 None**:换向量后端时沿用「缺字段就省略」的写法。
- **两集合 + 语义解析课程名**(`_resolve_course_name`)的检索语义不能丢。
- **去重按课程标题**(标题即 ID):多实例共享向量库后,重复摄取仍应「替换而非报错」。
- **`/api/*` 路由必须先于静态挂载**注册(否则 SPA 兜底吞掉 API)。

## 5. 不该过早做的事(YAGNI)

- 内部低并发场景**不要**一上来就 k8s + 三套中间件——先把阶段 1(Redis 多 worker)做了,通常就够。
- 不要为了"高级"引入消息队列 / 微服务拆分——本系统请求链路简单(检索 + 两次 LLM 调用),拆分只增加运维面。

## 6. 工作量速览

| 阶段 | 改动范围 | 粗估 | 解锁能力 |
|------|----------|------|----------|
| 1 Redis 会话+限流 | 2 个模块 + 配置 | 小 | 单机多 worker |
| 2 Postgres 用户库 | 1 个模块 + 配置 | 小~中 | 多机(账号一致) |
| 3 向量服务化 | vector_store + 摄取 + 部署 | 中 | 多机(检索一致、并发写) |
| 4 容器化+弹性 | 部署/CI/监控 | 中 | 自动扩缩、解耦摄取 |

> 一句话:**阶段 1 解锁多 worker,阶段 2+3 解锁多机,阶段 4 解锁弹性。** 按需逐步上,每一步都能独立交付。
